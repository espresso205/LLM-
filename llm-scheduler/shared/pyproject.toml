[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-scheduler-shared"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["pydantic>=2.0", "prometheus_client>=0.20.0", "starlette"]

[tool.setuptools.packages.find]
where = ["."]
