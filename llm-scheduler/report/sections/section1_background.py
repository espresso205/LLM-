"""（一）立项背景"""
from report.styles import add_section_heading, add_body, add_body_with_cite


def add_section1(doc):
    add_section_heading(doc, "（一）立项背景")

    add_body_with_cite(doc, [
        "近年来，以GPT-4、LLaMA、Qwen为代表的大语言模型（Large Language Model, LLM）"
        "取得了突破性进展，模型参数规模已达数十亿至数千亿级别",
        ("[1]", "cite"),
        "。这些模型在自然语言理解、生成、推理等任务中展现出强大能力，"
        "推动了AI技术在智能客服、内容创作、代码辅助等领域的广泛应用。"
        "然而，LLM推理过程对GPU显存的需求极为庞大，例如LLaMA-70B模型推理需要约140GB显存，"
        "单张GPU往往难以满足大模型的推理需求",
        ("[2]", "cite"),
        "。如何在有限的硬件资源下实现高效的分布式推理服务，成为当前学术界和工业界共同关注的热点问题。",
    ])

    add_body_with_cite(doc, [
        "当前，主流的LLM推理框架如vLLM",
        ("[3]", "cite"),
        "、Text Generation Inference (TGI)",
        ("[4]", "cite"),
        "、TensorRT-LLM",
        ("[5]", "cite"),
        "等在单节点推理优化方面已取得显著进展。"
        "其中vLLM提出的PagedAttention技术将KV Cache分页管理，显著降低了显存碎片；"
        "TensorRT-LLM通过算子融合和量化技术实现了极致推理吞吐。"
        "但在面对高并发、多用户的实际部署场景时，如何实现高效的分布式推理调度仍是一个亟待解决的问题，"
        "具体挑战包括：（1）多节点间的负载均衡问题；（2）节点故障的自动检测与转移；"
        "（3）系统运行状态的实时监控与告警；（4）GPU资源的弹性调度与利用率优化。",
    ])

    add_body_with_cite(doc, [
        "云原生技术（Docker、Kubernetes等）为AI推理的容器化和微服务化提供了技术基础",
        ("[6]", "cite"),
        "。微服务架构通过将系统拆分为独立部署的服务单元，实现了松耦合、高内聚的设计目标",
        ("[7]", "cite"),
        "。将LLM推理与云原生架构相结合，构建分布式推理调度引擎，"
        "对于提升GPU资源利用率、降低推理延迟、实现弹性扩展具有重要的理论价值和实际意义。"
        "本项目旨在设计并实现一个基于微服务架构的LLM云原生分布式推理调度引擎，"
        "通过多策略负载均衡、自动故障转移和实时监控告警机制，为LLM的高效部署提供完整的解决方案。",
    ])
