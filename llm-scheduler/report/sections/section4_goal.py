"""（四）结题预期目标"""
from report.styles import add_section_heading, add_body


def add_section4(doc):
    add_section_heading(doc, "（四）结题预期目标")

    add_body(doc, "本项目预期在结题时达到以下目标：")

    add_body(doc,
        "（1）系统功能完整性：完成包含API网关、调度引擎、推理节点和监控服务在内的"
        "完整分布式推理调度系统，支持多用户并发访问、多模型推理（Qwen3-0.6B、"
        "TinyLlama-1.1B、Qwen2.5-7B）、多策略负载均衡及运行时热切换。"
    )

    add_body(doc,
        "（2）高可用与容错：系统具备节点故障自动检测与转移能力，单节点故障不影响"
        "整体服务可用性，通过心跳监测（30秒超时）和重试机制（3次指数退避）保障请求"
        "成功率≥99%。"
    )

    add_body(doc,
        "（3）性能指标：调度决策延迟≤10ms，网关层认证与路由额外开销≤5ms，监控采集"
        "对系统性能影响可忽略（CPU占用<1%），三种调度策略在不同负载下均能实现合理的"
        "请求分配。"
    )

    add_body(doc,
        "（4）可扩展性：支持动态增删推理节点，新增节点可在30秒内完成自注册并开始"
        "接收请求，无需重启任何已有服务。Docker Compose一键部署，Prometheus+Grafana"
        "提供标准化监控能力。"
    )

    add_body(doc,
        "（5）学术成果：完成毕业论文一篇，包含完整的系统设计、关键技术实现（策略模式"
        "调度引擎、指数退避重试、心跳容错、告警引擎）、基准测试数据分析和文献综述，"
        "力争在LLM推理调度领域提出有价值的实践参考。"
    )

    add_body(doc,
        "当前项目已完成核心功能开发与集成测试，系统已可通过Docker Compose一键部署并"
        "稳定运行。后续工作主要集中在流式推理支持、GPU感知调度和性能优化三个方面，"
        "技术路线清晰，工作量可控。按照当前进度，项目按期完成的可能性较高。"
    )
